# 🚀 Project Setup Guide

## Frontend

To install dependencies and build the frontend:

```bash
cd static/overview-app
npm install
npm run build
```

## Forge App

**To install and deploy the Forge app to Jira:**

Install the app on Jira:
```bash
forge install
```
Deploy the app to Jira:
```bash
forge deploy
```
